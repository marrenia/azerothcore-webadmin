# Debian/Ubuntu: apt install python3-flask python3-pymysql python3-gunicorn
# Note: Debian's python3-gunicorn has no /usr/bin/gunicorn; run `python3 -m gunicorn`.
Flask>=2.0
PyMySQL>=1.0
gunicorn>=20.1
